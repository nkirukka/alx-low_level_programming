#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Description: print lower and uppercase alphabet
 *
 * Return: Always 0(Success)
 */

int main(void)
{
int i, j;
for (i = 97; i < 123; i++)
putchar(i);
for (j = 65; i < 91; i++)
putchar(j);
putchar(10);
return (0);
}
