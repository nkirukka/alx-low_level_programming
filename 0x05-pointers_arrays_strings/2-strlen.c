#include "main.h"
#include <stdio.h>

/**
 * _strlen - find length of string
 *
 * @s: s is a string
 *
 * Return: length
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
}
return i;
}
