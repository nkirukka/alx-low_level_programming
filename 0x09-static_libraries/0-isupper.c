#include "main.h"

/**
 * _isupper - return uppercase only
 *
 * @c: c is an integer
 *
 * Return: 1 if upper or 0 if not
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
